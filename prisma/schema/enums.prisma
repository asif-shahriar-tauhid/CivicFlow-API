enum Role {
    CITIZEN
    ADMIN
    STAFF
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum AuthProvider {
    CREDENTIAL
    GOOGLE
}

enum PaymentStatus {
    UNPAID
    PENDING
    COMPLETED
    FAILED
    REFUNDED
}

enum CaseType {
    COMPLAINT
    SERVICE_REQUEST
}
