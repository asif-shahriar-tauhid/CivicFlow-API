enum Role {
    CITIZEN
    ADMIN
    STAFF
}

enum UserStatus {
    ACTIVE
    BLOCKED
    DELETED
}

enum AuthProvider {
    CREDENTIAL
    GOOGLE
}

enum PaymentStatus {
    UNPAID
    PENDING
    COMPLETED
    FAILED
    REFUNDED
}

enum CaseType {
    COMPLAINT
    SERVICE_REQUEST
}

enum AppointmentStatus {
    PENDING
    CONFIRMED
    CANCELLED
    ONGOING
    COMPLETED
}

enum TechnicianVerificationStatus {
    PENDING
    APPROVED
    REJECTED
}

enum ScheduleStatus {
    DRAFT
    PUBLISHED
}
